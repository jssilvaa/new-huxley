import QtQuick
import QtQuick.Controls

Rectangle {
    id: mainWindow
    color: "transparent"
    radius: 50

    StackView {
        id: mainStack
        anchors.fill: parent
        // initialItem:
    }
}
